# In this we design LeNet architecture from scratch.

## Libraries used:
numpy
tesorflow(only to import complete dataset of MNIST)
sklearn(to create confusion matrix, accuracy_score)
pandas(to create dataframe)
seaborn(to create heatmap from dataframe)
matplotlib(to plot heatmap)
pickle(to save kernel weights)
